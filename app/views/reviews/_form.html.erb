<%= @review.errors.full_messages.join(", ") %>


<%= form_for [@product, @review], remote: true do |f| %>
  <div>
    <%= f.label :body  %>
    <%= f.text_area :body %>
  </div>
<%= f.submit %>
<% end %>
